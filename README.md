
---

# 🎂 Interactive Birthday Greeting Card

A simple, elegant, and responsive **Happy Birthday** greeting card built with HTML and CSS. This project features a centralized card design, a festive GIF animation, and stylized typography with smooth fade-in effects.

## 🚀 Features

* **Centered Layout:** Uses Flexbox to ensure the card is perfectly centered on any screen size.
* **Clean Design:** A soft pink theme with deep shadow effects for a "pop-out" 3D look.
* **Animated Elements:** Includes a CSS `fadeIn` animation for the cake imagery.
* **Cursive Typography:** Custom styled "Happy Birthday" text using CSS pseudo-elements.

## 🛠️ Built With

* **HTML5:** For the structural layout.
* **CSS3:** For styling, Flexbox positioning, and animations.
* **Tenor GIF API:** For the festive cake animation.

## 📂 Project Structure

* `index.html`: The main entry point containing the card structure.
* `style.css`: The stylesheet containing all visual designs and animations.

## 🖥️ How to Use

1. **Clone the repository:**
```bash
git clone https://github.com/chadhas282-afk/birthday-wish-card.git

```


2. **Open the project:**
Simply double-click the `index.html` file in your browser to view the card.

## 🎨 Customization

You can easily personalize this card by editing the `style.css` file:

* **Change Colors:** Update the `background-color` in the `body` or `.card` classes.
* **Change Text:** Modify the `content` property in `.Happy_Birthday::after` to change the greeting message.
* **Adjust Size:** Change the `width` in the `.cake-anim` class to resize the birthday GIF.

---
